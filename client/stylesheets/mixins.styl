// mixins

// generate prefixes
prefix(prop, val)
  -webkit-{prop} val
  -moz-{prop} val
  -ms-{prop} val
  -o-{prop} val
  {prop} val

box-shadow()
  prefix('box-shadow', arguments)

transform()
  prefix('transform', arguments)

user-select()
  prefix('user-select', arguments)
